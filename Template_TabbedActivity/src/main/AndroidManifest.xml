<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.template.tabbed">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SwipeViewsActivity"
            android:label="@string/activity_swipe_views"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ActionBarTabsActivity"
            android:label="@string/activity_actionbar_tabs"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".ActionBarSpinnerActivity"
            android:label="@string/activity_actionbar_spinner"
            android:theme="@style/AppTheme.NoActionBar"
            />


    </application>

</manifest>